import { MyHideIfUnauthorizedDirective } from './my-hide-if-unauthorized.directive';

describe('MyHideIfUnauthorizedDirective', () => {
  it('should create an instance', () => {
    const directive = new MyHideIfUnauthorizedDirective();
    expect(directive).toBeTruthy();
  });
});
